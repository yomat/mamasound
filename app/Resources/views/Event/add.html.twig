<h1>Nouvel événement</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
		{#<input type="submit" value="Create" />#}
	{{ form_end(form) }}

<ul>
	<li>
		<a href="{{ path('app') }}">Back to the list</a>
	</li>
</ul>
